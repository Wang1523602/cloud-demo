spring:
  application:
    name: service-product
  cloud:
    seata.enable: false
    nacos:
      config:
        import-check:
          enabled: false
        server-addr: 127.0.0.1:8848
    sentinel:
      transport:
        dashboard: localhost:8080
      eager: true
server:
  port: 9000
seata:
  enabled: false